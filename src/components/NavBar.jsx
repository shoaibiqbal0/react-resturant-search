import { Flex, HStack, Image } from "@chakra-ui/react";
import React from "react";

const NavBar = () => {
  return (
    <Flex>
      <HStack>
        <Image />
      </HStack>
    </Flex>
  );
};

export default NavBar;
